with parallel, exception_show, types_example, string_type, Figures, input_output, hello_world;

use parallel, exception_show, types_example, string_type, Figures, input_output, hello_world;


procedure Main is

begin
   -- Hello; --helloworld
   -- Compute;
   -- Print_Squares;
   Loop_While;
   -- talking; -- Babblers
   -- show; -- Excpetion show
   -- print_types; -- Excpetion show
   -- string_example;
   -- Main_Figures; --inheritance example
   -- Make_Hi;
   -- Open_Hi;
end Main;
